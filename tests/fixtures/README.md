# Test Fixtures

Keep small, deterministic, hand-authored Factorio dump fixtures in this
directory.

Each fixture should contain only the prototype data needed by its tests. Prefer
readable JSON over copied production dumps, and document intentionally invalid
fields in the test that consumes them.
